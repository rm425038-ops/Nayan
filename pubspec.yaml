name: nayan
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^1.10.6
  firebase_auth: ^3.3.21
  cloud_firestore: ^3.1.22

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
